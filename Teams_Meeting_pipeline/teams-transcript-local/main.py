"""Entry point. Run from this folder, e.g.  python main.py message.json
All the logic lives in the `shared` package (see shared/runner.py)."""

from shared.runner import run

if __name__ == "__main__":
    run()
